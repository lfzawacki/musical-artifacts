- content_for :title, t('.new')

%section#form
  .container

    .row
      .col-lg-12.text-center
        %h2= t('.new')
        %hr.star-primary

    = render 'form'
    = link_to 'Back', artifacts_path
