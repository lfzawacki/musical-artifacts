- content_for :title, t('.new')

%section#form
  .container

    .row
      .col-lg-12.text-center
        %h2= t('.new')
        %hr.star-primary

    = render 'form'

    .row.nav-links.pad-top
      = link_to t('_other.back'), artifacts_path