language: ruby
bundler_args: "--without production development"

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml
  - cp config/settings.yml.example config/settings.yml

script: bundle exec rake db:migrate; bundle exec rake db:reset; bundle exec rake

addons:
  code_climate:
    repo_token: 81485df48b297bfd0c207ff704b5524fd3963fcba289eb06094fa258691ef939